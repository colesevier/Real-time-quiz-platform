# Tools & Versions

- Java 21 (Eclipse Temurin or similar)
- Maven 3.8+
- Spring Boot 3.x (see `pom.xml`)
- MySQL 8
- Docker & Docker Compose (optional for dev)
- jBCrypt for password hashing (org.mindrot:jbcrypt)
- Jackson for JSON (com.fasterxml.jackson.databind)

Developer helpers
- IntelliJ IDEA or VS Code (Java extensions)
- curl, mysql CLI for DB operations
- Optional: Testcontainers for integration testing in CI
